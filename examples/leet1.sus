function twoSum(nums: number[], target: number)
	for(i = 0, call(len, nums))
		for(j = i + 1, call(len, nums))
			if(nums[i] + nums[j] == target)
				return [i, j]
			end
		end
	end
	return [0, 0]
end

print(call(twoSum, [2, 7, 11, 15], 9))
print(call(twoSum, [3, 2, 4], 6))
print(call(twoSum, [3, 3], 6))
