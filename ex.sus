// This function prints `69, "nice"` when evaluated
function exampleFunc(hello: number, world: string)
	if(hello * 3 == 69)
		print(hello * 3, world)
	end
end

call(exampleFunc, 12 * 4 / 2 - 1, "nice")
